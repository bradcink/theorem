<div class="intro">
	<div class="intro-text">
		<% if user_signed_in? %>
		 <li><%= link_to 'Logout', destroy_user_session_path, :method=>'delete' %></li>
		 <% else %>
		<li class="btn btn-primary btn-large"><%= link_to '<i class="fa fa-facebook-square"></i> Sign in with Facebook'.html_safe, user_omniauth_authorize_path(:facebook) %></li>
		<% end %>
	</div>
</div>
<li><%= link_to 'Login', new_user_session_path %></li>
